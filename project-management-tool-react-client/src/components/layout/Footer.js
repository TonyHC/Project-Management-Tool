import React from "react";

const Footer = () => {
  return (
    <footer className="footer mt-auto py-3 text-center">
        <span className="text-muted">&copy; Tony Chao</span>
    </footer>
  );
};

export default React.memo(Footer);